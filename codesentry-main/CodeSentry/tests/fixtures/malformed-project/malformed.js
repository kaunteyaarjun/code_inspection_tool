// This file contains intentionally malformed code

function brokenFunction(
  // Missing closing parenthesis
  console.log('This function is broken');
}

// Invalid syntax
const obj = {
  key: 'value',
  missing: 'comma'
  anotherKey: 'anotherValue'
}

// Unclosed string
const str = 'This string is never closed;

// Invalid function call
brokenFunction(;

// Missing semicolons and broken code
if (true {
  console.log('This will never run');
}

// Invalid array
const arr = [1, 2, 3,];

// Broken object
const broken = { a: 1, b: 2, c: };